<nav class="navbar-main">
  <a class="logo-navbar" href="{{link '/accueil/' }}"><img src="{{link '/assets/img/coch-logo2.png' }}" alt="CoCh" /></a>
  <ul class="nav nav-menu">
    <li>
      <a class="toggle-sidebar" title="Menu"><span class="icon icon-hamburger"></span></a>
    </li>
  </ul>
  <ul class="nav nav-break">
    {{#each navigation }}
    <li class="{{#is ../title this.name }}active{{/is}}{{#is this.subnav ../page.category }}{{#if this.subnav}}active{{/if}}{{/is}}{{#is this.subnav ../page.top_category}}{{#if this.subnav }} active{{/if}}{{/is}}{{#if this.subnav }} dropdown{{/if}}">
      <a href="{{link url }}">{{ name }} {{#if this.subnav}}<span class="icon icon-angle-down"></span>{{/if}}</a>
      {{#if this.subnav }}
      <ul class="dropdown-list">
        {{#each ../../pages }}
        {{#is data.category ../this.subnav}}
        <li {{#if data.subnav }}class="dropdown"{{/if}}>
          <a href="{{ baseurl }}{{ ../../url }}{{ basename }}/">{{ data.title }} {{#if data.subnav }}<span class="icon icon-angle-right caret"></span>{{/if}}</a>
          {{#if data.subnav }}
          <ul class="dropdown-list">
            {{#each ../../../../../pages }}
            {{#is data.category ../data.subnav }}
            <li><a href="{{ baseurl }}{{ ../../../../../url }}{{ ../../basename }}/{{ basename }}/">{{ data.title }}</a></li>
            {{/is}}
            {{/each}}
          </ul>
          {{/if}}
        </li>
        {{/is}}
        {{/each}}
      </ul>{{/if}}
    </li>
    {{/each}}
  </ul>
</nav>